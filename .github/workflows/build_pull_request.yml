name: Build on Pull Request
on:
  pull_request:
    branches:
    - main
    - stable
env:
  CACHE_VERSION: xxxxx1

permissions:
  contents: read

jobs:
  build:
    uses: "./.github/workflows/build_and_test.yml"
